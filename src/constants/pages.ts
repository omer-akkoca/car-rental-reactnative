import { stackTypes } from "../types"

const pages: {
    onboarding: stackTypes,
    car_list: stackTypes,
    car_details: stackTypes,
    map_details: stackTypes,
} = {
    car_details: "car_details",
    car_list: "car_list",
    onboarding: "onboarding",
    map_details: "map_details",
}

export default pages;